import 'package:flutter/material.dart';

Widget dot({Color color = Colors.grey}) => Container(
  margin: EdgeInsets.symmetric(horizontal: 3),
  child: Icon(Icons.fiber_manual_record, size: 4.5, color: color,)
);